# config 目录说明

这里放本机或服务器自己的配置文件。真实账号、密码、token 不提交到 Git。

仓库保留的模板：

- `token.example.json`
- `quantzz_token.example.json`
- `settings.example.yaml`

本机或服务器实际使用的文件：

- `token.json`
- `quantzz_token.json`
- `settings.yaml`

真实文件已被 `.gitignore` 忽略。
